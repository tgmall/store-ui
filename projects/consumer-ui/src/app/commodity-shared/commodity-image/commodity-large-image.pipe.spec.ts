import { CommodityLargeImagePipe } from './commodity-large-image.pipe';

describe('CommodityLargeImagePipe', () => {
  it('create an instance', () => {
    const pipe = new CommodityLargeImagePipe();
    expect(pipe).toBeTruthy();
  });
});
